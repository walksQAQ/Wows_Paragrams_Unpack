# ============================================================
# Wows Paragrams Unpack —— PySide6 重建项目
# ============================================================
# 安装:
#   pip install -r requirements.txt
# 打包 (Nuitka):
#   pip install nuitka
#   运行 build.bat
# ============================================================

# 图形界面 GUI 相关依赖
PySide6>=6.5
matplotlib>=3.8

# 3D 模型查看器（.geometry 解析 + OpenGL 渲染）
numpy>=1.24
PyOpenGL>=3.1
meshoptimizer>=0.2

# 3D 模型导出（GLB / glTF 2.0 Binary：渲染模型 + 装甲模型）
pygltflib>=1.16
Pillow>=10.0
texture2ddecoder>=1.1

# 网络下载相关依赖
requests>=2.28

# 处理 PO/MO 文件翻译相关
polib>=1.2

# 打包构建相关依赖
nuitka>=2.0
zstandard>=0.25
setuptools-scm>=8