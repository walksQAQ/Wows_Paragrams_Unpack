# ============================================================
# Wows Paragrams Unpack — PyQt6 重构版依赖
# ============================================================
# 安装:
#   pip install -r requirements.txt
# 打包 (Nuitka):
#   pip install nuitka
#   参考 README.md 中的打包命令
# ============================================================

# ── GUI 框架 ──────────────────────────────────────────────
PyQt6>=6.5
PyQt6-Qt6>=6.5

# ── 网络请求 ──────────────────────────────────────────────
requests>=2.28

# ── PO/MO 文件解析 ────────────────────────────────────────
polib>=1.2

# ── 可选：图片水印工具 ────────────────────────────────────
# opencv-python-headless>=4.8
# （对应 embed_text_watermark.py，非核心功能）

# ── 打包工具（开发依赖） ──────────────────────────────────
# nuitka>=2.0
